# Project 3 — Voice Agent with Branch Logic: Grading Rubric

For each **criterion row**, choose the **one** achievement column that best matches the submission and award that cell's **points**. Sum the five rows (maximum **100** if every row is Proficient).

| **Criteria** | **Novice** | **Competent** | **Proficient** |
|--------------|------------|---------------|----------------|
| **Voice Agent Design Document** | **Points: 8** — Branch table incomplete or generic; router undefined; voice adaptations from Project 1 missing or vague. | **Points: 14** — All three branches defined and the router described, but entry/exit conditions are loose or the voice adaptations are stated without explanation. | **Points: 20** — Branch table complete with distinct entry/exit conditions for all three branches; router logic clearly described; the voice adaptations from Project 1 (conversational tone, dynamic inputs, audible handoff) are specific and explained. |
| **ElevenLabs build** | **Points: 6** — Share link missing or broken at grading time; branches collapsed into a single system prompt instead of built as workflow nodes; no Branch 2 knowledge base. | **Points: 11** — Share link works and the workflow has three branch subagents wired to a router, but transitions are loose (vague edge conditions) or the Branch 2 knowledge base is thin or poorly matched to the branch it's attached to. | **Points: 15** — Share link works at grading time; the **Workflow canvas screenshot** shows a router and three distinct branch subagents with clearly labeled transitions; the Branch 2 knowledge base document is attached to the right subagent node (visible in a screenshot of that node's Knowledge Base tab); the system prompt is slim (shared role/tone only, no routing logic). |
| **Order Lookup tool** | **Points: 10** — Tool not configured, or configured incorrectly (wrong URL, missing parameter); the tool is not attached to the Order Lookup subagent; the agent never successfully calls it. | **Points: 18** — Tool is configured and attached to the Order Lookup subagent, the agent calls it, but the parameter or description is sloppy, the agent mishandles the response, or the invalid-ID case was not tested. | **Points: 25** — The `lookup_order` webhook is configured correctly with the `order_id` parameter and attached *only* to the Order Lookup subagent (Inherit tools OFF, screenshot shows the tool on the node); the agent collects the ID, calls the tool, and reads back what was ordered for a valid ID (1001–1006); the invalid-ID case is tested and the agent refuses to invent an order; screenshots show the tool config, the node attachment, and a successful call. |
| **Test conversations** | **Points: 10** — Fewer than three branches tested, or transcripts missing routing/activation notes or pass/fail diagnosis. | **Points: 18** — All three branches tested, but diagnoses are thin, or knowledge-base/tool activation is not clearly assessed in one or more conversations. | **Points: 25** — All three branches tested with transcripts; routing is assessed; knowledge-base or tool activation is noted for each; the escalation branch is documented; each pass/fail diagnosis is specific. |
| **Voice and tool failure analysis** | **Points: 6** — One or more questions missing or answered with generic statements not tied to this build. | **Points: 11** — All three questions answered, but at least one stays general or is only loosely connected to the actual test results. | **Points: 15** — All three questions answered with specifics from actual test results; the tool question names what concretely closed the gap; a voice-specific metric is named in Question 1. |

*If every criterion were scored at the same column: Novice **40**, Competent **72**, Proficient **100**.*
